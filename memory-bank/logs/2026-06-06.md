---
created: 2026-06-06
modified: 2026-06-06
tags: [log, git, publishing, memory-bank, meta]
summary: Published vibe-actor/vibe-combat v2.0.0 histories to GitHub, synced vibe-common, then redesigned the Memory Bank on the allen-wiki model geared toward Foundry module development.
module: shared
references: [../knowledge/AGENTS.md, ../AGENTS.md]
---

# 2026-06-06

## Publish vibe modules to GitHub

- Confirmed `vibe-actor`, `vibe-combat`, `vibe-common` were the only git repos
  under `Foundry VTT/Data/modules`; only `vibe-common` had a remote.
- Found local `master` histories (v2.0.0 / Foundry v14 migration, started
  2026-04-24) were **unrelated** to the GitHub `main` histories (old v1.3.0
  line, Feb 2026) — no common ancestor.
- Added a `.gitignore` to actor and combat (logs/test artifacts/PNGs except
  `icon.png`); committed the full previously-untracked v2.0.0 source trees.
  Verified no real secrets staged (only `.env.example`).
- Renamed local `master` → `main` and **force-replaced** GitHub `main` for
  both actor and combat. Old releases remain reachable via `v1.1.0`–`v1.3.0`
  tags. Pushed `vibe-common`'s 1 pending commit.
- All three repos now connected and in sync.

## Memory Bank redesign (allen-wiki model, Foundry-dev)

- Old `memory-bank-protocol` pointed at a `vibe-common/memory-bank/` tree
  that never existed (dangling `current-state.md` / `progress.md` /
  `NOTES_NEXT_SESSION.md`). Redesigned it on the
  [allen-wiki](file:///D:/Code%20Projects/allen-wiki) logging/knowledge model.
- Design centralized in `vibe-common/memory-bank/`; spec at (gitignored)
  `docs/superpowers/specs/2026-06-06-memory-bank-design.md`.
- Scaffolded areas: [`logs/`](AGENTS.md), [`knowledge/`](../knowledge/AGENTS.md),
  [`projects/`](../projects/AGENTS.md), [`scratch/`](../scratch/AGENTS.md)
  (gitignored), each with its own `AGENTS.md`.
- Rewrote the [`memory-bank-protocol`](../../.agents/skills/memory-bank-protocol/SKILL.md)
  skill: boot order + session-closing protocol, frontmatter schema with a new
  `module:` field, date-verification rule.
- Migrated the 12 "Known Gotchas" out of `vibe-common/AGENTS.md` into
  individual tagged `knowledge/` entries (foundry-api, application-v2,
  styling, ai-services); replaced the AGENTS.md block with a pointer and added
  a "Memory Bank" section. Knowledge entries written by three parallel sonnet
  subagents.

## Branch reconciliation & cleanup

- Discovered `vibe-common`'s GitHub **default branch was `main`** (stale v1.x +
  an old `memory-bank/core`+`technical`+`NOTES_NEXT_SESSION` tree), while all
  current work lived on `master`. The old reference files were one-line stubs —
  nothing worth salvaging.
- Renamed local `master` → `main`, force-replaced `origin/main` with the current
  work, and deleted the stale `origin/master`. Default branch stays `main`.
- Deleted the legacy `dev-phase2-polish` branch from **all three** repos.
- End state: `vibe-common`, `vibe-actor`, `vibe-combat` each have a single clean
  `main` branch holding the v2.0.0 work; old releases survive via `v1.x` tags.

## Next session

- Optional lint pass: migrate module-specific gotchas (e.g. vibe-actor
  `adjustActor` deletes all items; vibe-combat specifics) into `knowledge/`
  tagged by `module:`.
- Backfill `projects/` if/when a multi-session initiative starts.
